#include
#include

/**
 *
 *
 *
 *
 *
 */


int (getspec(char *s))(*va_args)

	spec identifiers[] = {
		{"d", printdigit},
		{"c", printchar}.
		{"s", printstr}
		{NULL, NULL}
	}
int i
i = 0
while (identifiers[i]. != NULL)
{
	if (identifiers[i]. [0] == *s)

		return (identifiers[i].f) or (0)
